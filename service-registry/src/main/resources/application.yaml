Server:
    port: 8761
    
Spring:
    application:
        name: service-registry
        
        
        
euraka:
    instance:
        hostname: localhost
        client:
            register-with-eureka: false 
            fetch-registry: false
            serviceUrl:
                devaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    
        